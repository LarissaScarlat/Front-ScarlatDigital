{
  // "files": ["src/index.js"], // Add file paths if needed, e.g. "files": ["src/index.js"]
  "references": [
    // Uncomment the following if tsconfig.node.json exists:
    // { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}